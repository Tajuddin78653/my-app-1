pipeline{
    agent { 
            label "Slave-linux" 
          }
    
    stages{
      stage("checking for Maven Build"){
        steps{
          sh "mvn clean package"
        }
      }
    }
}
